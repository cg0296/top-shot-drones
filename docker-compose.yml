services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    ports:
      - '5433:5432'
    environment:
      POSTGRES_USER: topshot
      POSTGRES_PASSWORD: topshot
      POSTGRES_DB: topshot_dev
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
